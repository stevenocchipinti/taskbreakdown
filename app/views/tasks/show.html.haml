%p#notice= notice

%h1= @task.name

%p
  = @task.description

= render partial: 'task_list', locals: {tasks: @task.children}

= link_to "Edit", edit_task_path(@task), :class => "button"
= link_to "New Subtask", new_task_path(parent_id: @task.id), :class => "button"
= link_to "Back", tasks_path, :class => "button"
